{{#if hasRecords}}
    <table class="table">
        <tr class="table-header">
            <th>ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Gender</th>
            <th>DOB</th>
            <th>Actions</th>
        </tr>
        {{#each}}
            {{#unless isNew}}
                <tr>
                    <td>{{id}}</td>
                    <td>{{firstName}}</td>
                    <td>{{lastName}}</td>
                    <td>{{gender}}</td>
                    <td>{{dateOfBirth}}</td>
                    <td>
                        <button class="btn btn-default" {{action 'editItem' this}}>Edit</button>
                        <button class="btn btn-default" {{action 'deleteItem' this}}>Delete</button>
                    </td>        
                </tr>
            {{/unless}}
        {{/each}}
    </table>
{{else}}
    <div class="alert alert-info">
        <em>No visits found.  Create a new visit record?</em> <button class="btn btn-primary" {{action 'newItem'}}>New Visit</button>
    </div>
{{/if}}
