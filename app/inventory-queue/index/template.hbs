{{#if this}}
    <table class="table">
        <tr class="table-header">            
            <th>Name</th>
            <th>Status</th>
            <th>Quantity</th>
            <th>Requested By</th>
            <th>Actions</th>
        </tr>
        {{#each}}
            <tr>
                <td>{{inventoryItem.name}}</td>
                <td>{{status}}</td>
                <td>{{quantity}}</td>
                <td>{{requestedBy}}</td>
                <td>            
                    <button class="btn btn-primary" {{action 'fulfill' this}}>Fulfill</button>
                </td>
            </tr>            
        {{/each}}
    </table>
{{else}}
    <div class="alert alert-info">
        <em>No requests found.  Create a new request?</em> <button class="btn btn-primary" {{action 'newItem'}}>New Request</button>
    </div>
{{/if}}