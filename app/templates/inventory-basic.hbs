<div class="row">
  {{#unless model.isNew}}
    <div class="form-group col-sm-2">
      <label>{{t 'labels.id'}}</label>
      {{input class="form-control test-item-id" value=model.friendlyId type="text" disabled=true }}
    </div>
  {{/unless}}
  {{em-input property="name" label=(t 'labels.name') class="required  col-sm-6 test-inv-name"}}
  {{inventory/rank-select property="rank"}}
  {{#unless model.isNew}}
    <div class="form-group col-sm-2">
      <label>{{t 'labels.quantity'}}</label>
      <p class="form-control-static">{{model.quantity}}</p>
    </div>
  {{/unless}}
</div>

{{em-text label=(t 'labels.description') property="description" rows=1 }}
<div class="row">
  {{em-select label=(t 'labels.type') property="inventoryType"
    content=inventoryTypes
      class="required col-sm-4 test-inv-type"
      prompt=" "
  }}
  {{em-input property="crossReference" label=(t 'inventory.labels.crossReference') class="col-sm-8 test-inv-cross"}}
</div>
<div class="row">
  {{em-input property="reorderPoint" label=(t 'inventory.labels.reorderPoint') class="col-sm-3 test-inv-reorder"}}
  {{number-input property="price" label=(t 'inventory.labels.salePricePerUnit') class="col-sm-3 test-inv-price"}}
  {{em-select label=(t 'inventory.labels.distributionUnit') class="col-sm-3 required test-inv-dist-unit"
    property="distributionUnit"
    content=unitListForSelect
    prompt=" "
  }}
</div>
