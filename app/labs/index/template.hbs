{{#item-listing paginationProps=paginationProps }}
  {{#if hasRecords}}
    <table class="table">
      <tr class="table-header">
        <th>{{t 'labels.dateRequested'}}</th>
        <th>{{t 'labels.patient'}}</th>
        <th>{{t 'labels.requestedBy'}}</th>
        <th>{{t 'labs.labels.lab_type'}}</th>
        <th>{{t 'labels.notes'}}</th>
        {{#if showActions}}
          <th>{{t 'labels.actions'}}</th>
        {{/if}}
      </tr>
      {{#each model as |lab|}}
        {{labs/index/labs-list-item
          lab=lab
          action='editItem'
          showActions=showActions
          canEdit=canEdit}}
      {{/each}}
    </table>
  {{else}}
    <div class="alert alert-info">
      <p>{{t 'labs.messages.no_items_found'}} {{#if canAdd}}<a href="#" {{action 'newItem'}}>{{t 'labs.messages.create_new_record'}}</a>{{/if}}</p>
    </div>
  {{/if}}
{{/item-listing}}
