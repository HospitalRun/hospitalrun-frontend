{{#item-listing paginationProps=paginationProps }}
  {{#if hasRecords}}
    <table class="table">
      <tr class="table-header">
        <th class="col-xs-2">{{t 'models.lab.labels.requestedDate'}}</th>
        <th class="col-xs-2">{{t 'models.lab.labels.patient'}}</th>
        <th class="col-xs-2">{{t 'models.lab.labels.requestedBy'}}</th>
        <th class="col-xs-2">{{t 'models.lab.labels.labType'}}</th>
        <th class="col-xs-2">{{t 'models.lab.labels.notes'}}</th>
        {{#if showActions}}
          <th class="col-xs-2">{{t 'labels.actions'}}</th>
        {{/if}}
      </tr>
      {{#each model as |lab|}}
        {{labs/index/labs-list-item
          lab=lab
          action='editItem'
          showActions=showActions
          canEdit=canEdit}}
      {{/each}}
    </table>
  {{else}}
    <div class="alert alert-info">
      <p>{{t 'labs.messages.noItemsFound'}} {{#if canAdd}}<a href="#" {{action 'newItem'}}>{{t 'labs.messages.createNewRecord'}}</a>{{/if}}</p>
    </div>
  {{/if}}
{{/item-listing}}
