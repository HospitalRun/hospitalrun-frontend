{{#item-listing paginationProps=paginationProps }}
  {{#if model}}
    <table class="table">
      <tr class="table-header">
        <th>{{t 'models.labs.labels.requestedDate'}}</th>
        <th>{{t 'models.labs.labels.labDate'}}</th>
        <th>{{t 'models.labs.labels.patient'}}</th>
        <th>{{t 'models.labs.labels.requestedBy'}}</th>
        <th>{{t 'models.labs.labels.labType'}}</th>
        <th>{{t 'models.labs.labels.result'}}</th>
        <th>{{t 'models.labs.labels.notes'}}</th>
      </tr>
      {{#each model as |lab|}}
        {{labs/completed/labs-completed-list-item lab=lab}}
      {{/each}}
    </table>
  {{else}}
    <div class="alert alert-info">
      <p>{{t 'labs.messages.noCompleted'}}</p>
    </div>
  {{/if}}
{{/item-listing}}
